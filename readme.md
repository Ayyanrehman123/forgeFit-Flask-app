to run the app>>


. **Install dependencies:**

   Ensure you have `pip` installed and then run:

   ```bash
   pip install -r requirements.txt
   ```

 ```bash
   pip install -r requirements.txt

   ```
